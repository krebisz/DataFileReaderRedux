Agnostic (Personal) Health Data Interpreter
-Meant to be a quick and dirty application to accumulate personal health data from multiple (current) sources in an agnostic way
-The info is to be sorted into heirarchical forms, that then can be stored in a relational (as opposed to non-relational) databases, for use with BI tools
